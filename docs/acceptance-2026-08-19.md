# 验收报告 — 一站式写作台（2026-08-19）

> 范围：一句话生成规范格式标书 / 外汇申报单 / 实验报告的功能拓展系列（main `f74f63b → ff77118`，共 14 提交）。
> 方法：node:test 全量套件（114 例）+ mock LLM 端到端 + computer-use 实机验收（应用 UI / Microsoft Word / 安装包）。

## 交付清单

| 切片 | 提交 | 内容 |
|---|---|---|
| docgen 生成链路 | `d4b03e6` | `src/docgen/`：markdown → 排版组件 → 唯一 seam `applyEdits` 产出 docx；agent 工具 `doc_generate` |
| 标书/外汇单规则集 | `3e4ea28` | `bid-default`（大章分页/报价表数值列右对齐）+ `fx-form-default`（表单高密度）；规则集画廊 4 套切换；遍历校验测试 |
| 一句话生成接线 | `f329430` | doc_generate 入 agent 白名单（修复注册未加载 bug）；系统提示词生成模式 + 三类文档领域结构；landing 一键生成三入口 |
| 验收修复 + 演示预置 | `66a06b8` | landing 错误就地提示（修复静默失败）；`scripts/seed-demo-docs.mjs` |
| mock e2e 生成链路 | `04072db` | 无凭证验证「一句话 → doc_generate → 版本链 v1 → 事件流契约」9 断言 |
| 金额大写工具 | `c5d2a2e` | `amount_words`（字符串十进制精确，财务零衔接规范），提示词强制大写栏走工具 |
| 预览修复 | `4f23377` | 摘除 `?x2t-only` 诊断残留，OnlyOffice 完整只读渲染恢复（此前"空白"实为调试旗标未拆） |
| 填字段出标书 | `b4b2d28` | agent 第 8 工具 `template_instantiate`；占位符标书模板（8 字段） |
| 纯本地出稿路径 | `4a0a84f` | landing「从模板填字段出稿」：选模板 → 填占位符 → 实例化 → 预览，零模型依赖 |
| 外汇单占位符模板 | `b38e87b` | 境外汇款申请书 18 占位符；表格单元格占位符提取/填充回归断言 |
| 安装包 | `4f7dbb8` | electron-builder NSIS 出包（约 740MB），npmmirror 镜像构建说明 |
| CI 门禁 | `d07a31a` | GitHub Actions 双平台（windows/ubuntu）全量测试，首跑双绿 |
| 收入申报单 + 实验报告验收 | `ff77118` | 涉外收入申报单 18 占位符；三类文档 Word 端验收 3/3 |

## 验收证据

- **Word 实机（兼容性模式打开，均正常）**：
  - 投标文件：标题黑体一号居中；大纲导航完整（一~六级）；报价表全边框、表头加粗居中、金额列右对齐；大章自动分页；共 7 页。
  - 境外汇款申请书：表单字段表全边框、表头黑体加粗、标题二号居中。
  - 实验报告：黑体二号标题、七章大纲导航、正文首行缩进 2 字符。
- **应用 UI（computer-use 驱动）**：landing 一键生成填入链路；生成/实例化成功自动选中新文档并刷新预览；模板抽屉占位符计数正确；「从模板填字段出稿」全链路（填"智慧园区管理系统建设项目（二期）"→ 预览正确替换、未填字段保留占位符）；下载 docx 另存为落盘字节一致；无 key 时错误提示可见。
- **OnlyOffice 预览**：生成标书 7 页即时渲染，工具栏字体/字号框与规则集一致。
- **打包产物**：`out/win-unpacked/排版工作台.exe` 免安装启动验收通过。
- **CI**：run 32162924244，windows-latest / ubuntu-latest 均 success。

## 已知边界与待办

- **真实 LLM 链路未复验**（本机无 API key）：mock e2e 已覆盖同构链路；真实对话验收待凭证。配置方式见使用指南。
- **WPS 端未验**（本机未安装）：发布前需人工在 WPS 打开生成文档确认版式（spec 首要风险收口）。
- 超大文档的 x2t wasm 内存边界未回归（`?x2t-only` 调试开关保留）。
- 安装包图标/签名：`build/icon.ico` 已备（本次提交），下次出包生效；代码签名属后续项。
- 应用内「演示」文档重复条目：seed 文档条目非幂等（模板幂等），多次执行会在最近文档列表产生重复演示文档，仅影响演示环境。

## 内部推广建议路径

1. 无模型环境也能用：从模板填字段出稿 + 一键重排，纯本地。
2. 有网关/key：AI 一句话生成 + 对话精调。
3. 公司格式落地：上传公司范本 docx → 自动反推规则集 → "按《XX 范本》排"。
